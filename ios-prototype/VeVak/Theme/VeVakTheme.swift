import SwiftUI

// Mirrors the Android VeVak identity without forcing fixed foreground colors.
enum VeVakTheme {
    static let blue = Color(red: 0x35 / 255.0, green: 0x67 / 255.0, blue: 0xC8 / 255.0)
    static let teal = Color(red: 0x1E / 255.0, green: 0x9F / 255.0, blue: 0x9A / 255.0)
    static let warm = Color(red: 0xD7 / 255.0, green: 0x7A / 255.0, blue: 0x5C / 255.0)
}
